describe('HMD Website Tests', () => {
  beforeEach(() => {
    cy.visit('https://www.hmd.com/en_pk');

    // Accept cookies if the banner exists
    cy.get('gravitoLightCMP-layer1-introductionText').should('exist').within(() => {
      cy.contains('Accept all').click();
    });
  });

  it('should load the homepage correctly', () => {
    // Assert that the homepage loads and contains expected elements using XPath
    cy.xpath('/html/body/div[1]/section[1]/div/div/div[1]/div/h2/span[1]/span[1], "Human Mobile Devices")]').should('exist');
    // Add more assertions as needed for other elements
  });
  // Add more tests for other functionalities as required

});
